// Test configuration file
// Centralize hardcoded values for easier testing

export const TEST_CONFIG = {
  // Mock user ID for demo/testing - in real app, get from auth context
  DEMO_USER_ID: 2,
} as const;

export default TEST_CONFIG;
